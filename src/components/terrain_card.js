import React, { Component } from 'react';

export default class TerrainCard extends Component {
  constuctor(props) {
    super(props);
  }

  render() {
    return(
      <div className="terrain-card container">
        <div>This works</div>
      </div>
    );
  }
}
